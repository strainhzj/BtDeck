<?xml version="1.0" encoding="utf-8"?>
<!-- 服务器列表：卡片式列表行（分隔交给卡片边距，listSelector 透明避免方角
     选中框盖住圆角卡片、按压反馈由行卡片自身 foreground ripple 提供）+
     翡翠绿主操作按钮。id 与旧版一致（server_list/btn_add_server）。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/btdeck_bg_secondary"
    android:orientation="vertical">

    <ListView
        android:id="@+id/server_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:cacheColorHint="@android:color/transparent"
        android:clipToPadding="false"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:listSelector="@android:color/transparent"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp"
        android:scrollbars="none" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_add_server"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:text="@string/add_server"
        android:textSize="16sp"
        app:backgroundTint="@color/btdeck_primary"
        app:cornerRadius="12dp"
        app:rippleColor="@color/btdeck_primary_deep" />
</LinearLayout>
