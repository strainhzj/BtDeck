请为 BtDeck 项目生成一个诊断脚本，用于获取准确的系统状态信息。

## 脚本要求

创建 `backend/scripts/diagnose.py`，运行后输出以下诊断信息：

### 1. 配置系统诊断

- 检测 `app/config.py` 和 `app/core/config.py` 的实际值
- 输出两个配置中 SECRET_KEY、ACCESS_TOKEN_EXPIRE_MINUTES、ALGORITHM 的值
- 检测是否设置了 SECRET_KEY 环境变量
- 检测 DATABASE_URL 环境变量是否设置，是否被代码消费
- 输出实际使用的 DATABASE_PATH
- 列出所有从 `app.config` import settings 的文件
- 列出所有从 `app.core.config` import settings 的文件

### 2. 数据库诊断

- 检测数据库文件是否存在
- 输出数据库文件大小
- 检测 alembic_version 表的当前版本
- 检测是否有 production_complete_schema.sql 初始化痕迹（版本号 9aea25308aff）
- 列出所有表及行数（种子表、用户表、定时任务表等关键表）
- 检测 SQLite WAL 模式是否启用

### 3. 定时任务诊断

- 列出所有 cron_task 记录
- 按 task_type 分组统计数量
- 对于 task_type=0,1,2,3（Shell/CMD/PowerShell/Python 脚本），输出：
  - 任务名称
  - 是否启用
  - executor 内容预览（前 100 字符）
  - 最后执行时间
- 对于 task_type=4（Python 内部类），输出 executor 类路径
- 检测是否有使用 enhanced_python_executor 的任务

### 4. 认证诊断

- 检测 JWT 库（python-jose vs jose vs pyjwt）
- 列出所有手动读取 X-Access-Token 的 endpoint 文件
- 列出使用 Depends(get_current_user) 的 endpoint 文件

### 5. 安全检查

- 检测默认管理员账号是否存在（admin/admin）
- 检测 CORS 配置
- 检测是否有 .env 文件被 git 跟踪
- 检测 backend/config/config.yaml 是否被 git 跟踪

### 6. 依赖诊断

- 检测 requirements.txt 中是否有冲突的 JWT 库
- 检测是否有 lock 文件（requirements.lock、pip-compile 输出等）

## 输出格式

使用表格和列表，便于阅读。输出到 stdout，同时保存到 `backend/docs/diagnostic-report.md`。

## 执行要求

1. 脚本必须能在当前环境运行（可能缺少部分依赖，需要 graceful 处理）
2. 如果某个模块 import 失败，记录错误但继续执行其他诊断
3. 使用 Python 标准库 + sqlite3，尽量不依赖项目代码
4. 如果需要访问数据库，使用只读连接
5. 不要修改任何业务代码
6. 脚本运行后自动执行诊断并输出结果

## 额外要求

生成脚本后，立即运行它并输出诊断结果。如果运行失败，分析原因并修复。
