# Runtime dependencies for Windows PyInstaller packaging.
# 结构（release-artifact-equivalence-gate W1 / G2）：
#   公共运行依赖一律来自 backend/requirements-lock.txt（pip-compile --generate-hashes），
#   本文件只允许追加 Windows 平台增量依赖（白名单见 scripts/release/check_dependencies.py
#   的 PLATFORM_EXTRAS_WHITELIST），禁止在此复制/覆盖公共依赖版本。
#   结构由 backend/tests/release/test_dependency_lock.py 强制；测试/lint/dev 工具
#   依旧不得进入打包 venv（锁只含 backend/requirements.txt 的运行时闭包）。
#
# 历史说明：2026-08-28 前本文件是 backend/requirements.txt 的手工全量拷贝，
# 导致 qbittorrent-api ~=2025.5.0 与主清单 ~=2025.2.0 分叉，且混入了运行时
# 零使用的 passlib/email-validator（passlib 属 dev 依赖，见 requirements-dev.txt）。

-r ../backend/requirements-lock.txt

pyinstaller~=6.20.0
pywebview~=5.4.0
