请将诊断脚本的关键检查逻辑集成到项目的代码质量检测流程中。

## 目标

创建可在 CI/CD 中运行的检测脚本，确保代码质量门禁。

## 任务

### 1. 创建 ruff 自定义规则插件

创建 `backend/scripts/lint_btdeck.py`，实现以下检查：

**配置系统检查**：
- 检测是否有文件直接 import `app.config.settings`（应使用 `app.core.config`）
- 检测是否有硬编码的 SECRET_KEY 值
- 检测是否有硬编码的密码或密钥

**认证检查**：
- 检测 endpoint 文件是否手动读取 `X-Access-Token`（应使用 dependency）
- 统计使用 `Depends(get_current_user)` vs 手动解析的比例

**安全检查**：
- 检测 `exec()` 调用
- 检测 `eval()` 调用
- 检测 `os.system()` 调用
- 检测 `subprocess.call(shell=True)` 调用
- 检测 SQL 字符串拼接（非参数化查询）

### 2. 创建 pytest 测试用例

创建 `backend/tests/test_architecture_constraints.py`，实现以下测试：

**配置系统测试**：
```python
def test_no_direct_config_import():
    """确保生产代码不直接 import app.config"""
    
def test_unified_token_expiry():
    """确保 ACCESS_TOKEN_EXPIRE_MINUTES 统一"""
```

**认证测试**：
```python
def test_auth_dependency_usage():
    """统计并验证认证方式使用比例"""
    
def test_no_manual_token_parsing():
    """确保新代码不手动解析 token"""
```

**安全测试**：
```python
def test_no_exec_calls():
    """确保没有 exec() 调用（除白名单外）"""
    
def test_no_sql_injection():
    """确保没有 SQL 字符串拼接"""
```

### 3. 创建 Makefile 或脚本

创建 `backend/Makefile` 或 `backend/scripts/run_checks.sh`，整合所有检查：

```makefile
.PHONY: lint test check-all

lint:
	python scripts/lint_btdeck.py
	ruff check app/

test:
	pytest tests/ -v

check-all: lint test
	@echo "All checks passed!"
```

### 4. 创建 CI 配置示例

创建 `backend/.github/workflows/code-quality.yml`（或说明文档），展示如何在 GitHub Actions 中使用。

## 输出要求

1. 用中文注释
2. 检测脚本要有清晰的错误提示，指出问题文件和行号
3. 测试用例要能独立运行
4. 生成集成报告保存到 `backend/docs/ci-integration.md`
5. 所有检测脚本必须能在当前环境运行（不依赖项目代码 import）
