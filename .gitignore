# ================================
# 全栈通用
# ================================
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ================================
# 环境与敏感文件
# ================================
.env
.env.local
.env.*.local

# ================================
# 构建产物
# ================================
dist/
build/
*.egg-info/

# ================================
# 日志与临时文件
# ================================
*.log
*.tmp
*.temp
*.bak
*.backup
# 发布门禁证据链例外：release/evidence/ 下的探针日志必须可提交
# （release-artifact-equivalence-gate G0～G10 证据可追溯性要求）
!release/evidence/**/*.log

# 发布生成物（按制品本地生成，不入库；正式制品与 manifest 由 CI 产出并归档）
release/build-info.json
release/source-manifest.json
release/frontend-asset-manifest.json
release/release-manifest.json

# ================================
# Python
# ================================
__pycache__/
*.py[cod]
venv/
.venv/
.venv-win/
.venv-packaging/
.venv-packaging-linux/
env/
.pytest_cache/
.coverage

# ================================
# Node.js
# ================================
node_modules/
.npm/

# ================================
# Docker 数据
# ================================
# 2026-08-24：删除 data/ 忽略规则（用户确认不再需要）。
# 该规则曾静默忽略 android/app/src/main/java/com/btdeck/companion/data/
# Kotlin 源码包，导致 a2f4e72 漏提交三文件、干净检出无法编译。
# 部署挂载目录请依赖 deploy 配置自身的路径约定，不要重新引入此规则。

# 子项目保留各自的 .gitignore 处理细节规则

# ================================
# 本地工具链与构建残留（勿提交）
# ================================
.zcode/
.spec-workflow/
.pnpm-store/
.docker_temp_*
btdeck-*.tar
nul
tools/putty/

# ================================
# 导入残留目录（历史命令注入痕迹，W7 清理后防再犯）
# ================================
import io;*/
.claude/

# ================================
# 部署凭据（build-and-export-images.bat 从此未跟踪文件读取，严禁入库）
# ================================
.btdeck-deploy-credentials.bat

# W2 发布构建生成物（由构建脚本生成进上下文/暂存区，不入库）
backend/build-info.json
frontend/build-info.json
release/build/

# 门禁证据目录（脚本/CI 运行时输出，2026-09-05 起不入库；权威留档=CI run artifact）
release/evidence/
