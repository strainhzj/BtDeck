请根据诊断结果和审查报告，执行第一阶段修复（止血阶段）。

## 修复任务

### 任务 1：配置系统合并

**目标**：将 `app/config.py` 改为 `app/core/config.py` 的转发层

**步骤**：
1. 读取 `backend/app/config.py` 和 `backend/app/core/config.py`
2. 将 `app/config.py` 改为兼容转发：
   - 保留原有的导入路径 `from app.config import settings` 继续可用
   - 实际指向 `app.core.config.settings`
   - 在文件顶部添加注释说明这是兼容层
3. 统一 SECRET_KEY 和 ACCESS_TOKEN_EXPIRE_MINUTES 的默认值
   - 保留 `app/core/config.py` 的值作为唯一来源
   - SECRET_KEY 默认值改为更安全的方式（环境变量优先，无则警告）
   - ACCESS_TOKEN_EXPIRE_MINUTES 统一为合理值（建议 60 分钟）
4. 确保不会引入循环导入

### 任务 2：认证依赖统一

**目标**：将分散的手动 token 解析统一为 FastAPI dependency

**步骤**：
1. 读取 `backend/app/auth/dependencies.py`，了解现有的 `get_current_user` 实现
2. 读取 2-3 个手动解析 token 的 endpoint 文件（如 `torrent_crud.py`、`downloader.py`）
3. 创建一个新的统一认证 dependency：
   - 支持 Header `X-Access-Token` 和 `Authorization: Bearer`
   - 统一错误返回格式
   - 提供用户信息对象
4. 不要立即修改所有 22 个文件，只创建 dependency 并在注释中说明迁移方式

### 任务 3：旧路由文件清理

**目标**：删除或归档 `backend/app/api/router.py`

**步骤**：
1. 确认无任何生产代码引用该文件
2. 将文件重命名为 `backend/app/api/_legacy_router_unused.py`
3. 在文件顶部添加警告注释
4. 更新 `backend/app/startup/routers_initializer.py` 中的相关注释

### 任务 4：定时任务安全加固

**目标**：禁止新建自定义脚本任务

**步骤**：
1. 读取 `backend/app/api/endpoints/cron_tasks.py`
2. 在创建任务的 API 中添加检查：
   - 如果 task_type 是 Shell/CMD/PowerShell/Python 脚本类型，返回错误
   - 只允许创建内置任务类型（Python 内部类、清理、导出等）
3. 对于更新任务的 API，同样添加限制
4. 添加配置项 `BTDECK_ALLOW_CUSTOM_SCRIPTS`（默认 False），允许管理员显式开启

### 任务 5：CORS 安全加固

**目标**：修复 CORS 配置

**步骤**：
1. 读取 `backend/app/core/config.py` 和 `backend/app/factory.py`
2. 修改 `ALLOWED_HOSTS` 默认值：
   - 开发环境：`["http://localhost:8080", "http://127.0.0.1:8080"]`
   - 生产环境：从环境变量读取，无则拒绝启动
3. 确保 `allow_credentials=True` 时不使用通配符

## 输出要求

1. 用中文撰写修改说明
2. 每个任务完成后输出修改的文件列表和关键变更
3. 生成修复报告保存到 `backend/docs/phase1-fixes.md`，包含：
   - 修改的文件清单
   - 每个修改的说明
   - 向后兼容性说明
   - 需要手动验证的项目
4. 不要修改测试文件（避免破坏现有测试）
5. 所有修改都要有清晰的注释说明意图
