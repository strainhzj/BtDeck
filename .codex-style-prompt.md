请对 BtDeck 项目的前后端风格统一性和 API 契约一致性进行深度评估。

## 评估范围

### 1. 后端 API 风格评估

**REST 规范性**：
- 统计所有 endpoint 的 HTTP 方法分布（GET/POST/PUT/PATCH/DELETE）
- 识别 RPC 风格接口（如 POST /downloader/getList、POST /tracker/addTracker）
- 识别动词滥用（如 DELETE /torrents/delete）
- 检查资源路径命名规范（单复数、大小写、连字符）
- 列出不符合 REST 规范的 endpoint，按严重程度分类

**HTTP 状态码使用**：
- 统计认证失败时的响应方式（HTTP 200 + code=401 vs HTTP 401）
- 统计业务错误的响应方式
- 检查是否有直接返回 HTTP 500 的情况
- 列出状态码使用不一致的 endpoint

**响应格式**：
- 检查 CommonResponse 的使用覆盖率
- 检查分页响应的字段名（list/total/pageSize vs list/total/page_size）
- 检查是否有 endpoint 返回非标准格式
- 列出响应格式不一致的 endpoint

### 2. 前端代码风格评估

**Vue 组件风格**：
- 统计 Class Component vs Options API vs Composition API 的使用比例
- 检查是否有 `<script setup>` 使用（项目禁止）
- 列出风格不一致的文件

**TypeScript 类型**：
- 统计 `any` 类型的使用次数和分布
- 检查 API 响应类型定义完整性
- 检查组件 Props 类型定义完整性
- 检查是否有 `as unknown as` 类型断言滥用

**API 调用方式**：
- 统计使用 `request` 封装 vs 直接 axios 调用的比例
- 检查错误处理方式（静默失败 vs 提示用户）
- 检查 token 传递方式一致性

**状态管理**：
- 统计 Vuex module 风格（decorator vs traditional）
- 检查是否有组件直接修改 store state（违反单向数据流）

### 3. 前后端 API 契约评估

**字段命名一致性**：
- 检查后端返回字段名 vs 前端使用字段名（snake_case vs camelCase）
- 检查分页参数名一致性
- 检查日期时间格式一致性
- 列出命名不一致的字段

**数据结构一致性**：
- 检查嵌套对象结构是否匹配
- 检查枚举值是否一致
- 检查可选字段处理是否一致

**错误处理契约**：
- 检查后端错误码定义 vs 前端错误处理逻辑
- 检查是否有前端硬编码错误码
- 检查错误消息格式一致性

**认证契约**：
- 检查 token 传递方式（Header name、格式）
- 检查 token 刷新机制
- 检查 401 处理流程一致性

## 输出要求

1. 用中文撰写
2. 保存到 `backend/docs/style-and-contract-audit.md`
3. 每个问题标注：
   - 位置（文件:行号）
   - 严重程度（🔴 严重 / 🟡 中等 / 🟢 轻微）
   - 影响范围
   - 修复建议
4. 提供统计表格（问题分布、类型分布等）
5. 给出统一方案建议和优先级排序
6. 不要修改任何代码文件
