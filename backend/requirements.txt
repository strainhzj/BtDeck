# BTDeck 后端生产依赖
# 仅包含运行时必需的依赖。开发/测试/类型检查工具见 requirements-dev.txt
# 拆分先例参考 deploy/requirements-{linux,windows}-package.txt
#
# uvicorn与pydantic安全版本升级 - 2025-11-13
# Uvicorn: 0.22.0 → 0.35.0 (+13版本)
# Pydantic: 2.9.2 → 2.12.4 (+4版本)
# Starlette: 0.27.0 → 0.38.6 (+11版本)
werkzeug~=3.1.3
# click 在 Windows 的传递依赖（colorama；platform_system=='Windows'），
# 必须显式入锁：Linux 生成的锁不含它，Windows --require-hashes 安装会因未锁定传递依赖失败
# （W3 CI 实测拦截）。带 sys_platform 标记，Linux 运行时不安装。
colorama==0.4.6; sys_platform == "win32"
uvicorn~=0.35.0
sqlalchemy~=2.0.15
setuptools~=75.8.0
click~=8.1.8
requests~=2.32.3
regex>=2024.11.6,<2027.0.0
pydantic~=2.12.4
# CVE-2024-47874（multipart 解析 DoS，修复于 starlette 0.40.0）：
# fastapi 0.115.6 起放宽 starlette 上限至 <0.42，配套升级（W13）。
# 历史组合 fastapi~=0.115.0 + starlette~=0.41.3 是矛盾锁定（pip 只能解析到
# fastapi 0.115.2，starlette 永远停留在受 CVE 影响的 0.38.x）。
fastapi~=0.115.6
pyotp~=2.9.0
qrcode~=8.1
pycryptodomex~=3.23.0
# 密码哈希运行时依赖（app/auth/security.py 直接 import bcrypt；passlib 仅 scripts/diagnose.py 使用）
bcrypt~=5.0.0
python-dotenv~=0.21.0
PyYAML~=6.0.1
gmssl~=3.2.2
# 2026-08-25：锁到生产实装版本（原声明 ~=2025.5.0 从未实装；2025.2.0 已生产验证，
# 升级需重新验证 FORCE_SCHEME_FROM_HOST/HTTPADAPTER_ARGS 行为）
qbittorrent-api~=2025.2.0
ping3~=4.0.8
pyjwt~=2.8.0
aiosqlite~=0.21.0
pillow~=11.1.0
transmission-rpc~=7.0.11
urllib3~=2.2.2
starlette~=0.41.3
alembic~=1.13.3
bencodepy~=0.9.5
apscheduler~=3.10.0
# tzlocal 在 Windows 的传递依赖（tzdata；platform_system=='Windows'），
# 必须显式入锁：Linux 生成的锁不含它，Windows 安装锁时 pip 会因未锁定的
# 传递依赖在哈希模式下失败（W3 CI 实测拦截，与 colorama 同型）。带平台标记，
# Linux 运行时不安装。
tzdata==2026.3; platform_system == "Windows"
python-multipart~=0.0.20
pydantic-settings~=2.12.0
croniter~=3.0.4
httpx~=0.28.0
packaging~=24.0
# 审计日志 Excel 导出（openpyxl 直写；pandas/numpy 已随 dual-mode-client
# Phase 1.3 依赖瘦身移除，全仓零 import 已核实）
openpyxl~=3.1.0
