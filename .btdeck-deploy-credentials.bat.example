@echo off
rem Template for BtDeck deploy credentials.
rem Copy to .btdeck-deploy-credentials.bat and fill in real values.
rem The real file is gitignored; never commit credentials to version control.
set "DEFAULT_SSH_PASSWORD=YOUR_SSH_PASSWORD_HERE"
set "DEFAULT_PLINK_HOSTKEY=SHA256:YOUR_PLINK_HOSTKEY_HERE"
