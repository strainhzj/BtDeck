# BtDeck 代码路线图

> BtDeck —— 统一管理多种 BitTorrent 客户端（qBittorrent / Transmission）的全栈 Web 应用（FastAPI + Vue 2 + TypeScript）。
> 本路线图是对**源码结构的有证据索引**，按"由粗到细"分三层渐进式披露，用于快速定位模块职责、调用关系与架构约定。

---

## 模块路由

```
BtDeck/
├── backend/          ← FastAPI 后端（Python 3.11+）
│   ├── app-root/        包根入口（main/factory/database/exception_handlers …）
│   ├── api/             HTTP 路由层（39 endpoints + schemas + models + responseVO，含 platform_capabilities 能力矩阵）
│   ├── services/        业务服务层 + 下载器/标签适配器（含 ratio/search 运行时、异步删除条目占用与查询排除）
│   ├── core/            基础设施（config / path_mapping / file_ops …）+ ⚠ 含 4 个孤儿文件（torrent_operations 已重写为 ratio 工具但仍 0 引用）
│   ├── contracts/ ✨    前后端共享机器可读契约（v1.0.6.27 新增：advanced_search JSON + 加载器）
│   ├── data-models/     ORM 模型 + repositories + schemas + 枚举 + 默认数据
│   ├── tasks/           定时任务 + scheduler + 后台任务
│   ├── domain/          领域目录（downloader / torrents / tracker / auth / user）
│   └── infra/           utils + startup + migrations + alembic（31 个 revision，最新 search/setting template preset_key 双迁移）
├── frontend/         ← Vue 2.6 + TypeScript 前端
│   ├── entry/           应用入口（main.ts / router.ts / permission.ts / App.vue / i18n/ 双语基础设施）
│   ├── api/             axios API 封装（16 个领域模块，2026-09-22 合并后 +mcp-settings/moviepilot）
│   ├── views/           页面视图（13 个 view 模块，Options API + class-component 并存）
│   ├── store/           Vuex（index.ts 空壳 + 4 个 getModule 自注册 + downloaderSettings 传统 namespaced）
│   ├── components-layout/  通用组件（AppLogo / LucideIcon / PageSizeCombobox / AdvancedSearchWorkspace）+ 布局骨架
│   ├── demo/            静态展示 Demo（typed fixture + 内存 store + 集中式请求分流）
│   └── utils-types/     工具 / 类型 / 常量 / 指令（v1.0.6.36 新增 clipboard 剪贴板回退）
├── android/          ← 伴侣模式 App（dual-mode-client Phase 2 MVP：向导/服务器 profile/健康检查/同源 WebView）
├── deploy/           ← 多部署模式（Docker / PyInstaller / Inno Setup / fpm；v1.0.6.28 Dockerfile 镜像源参数化）
├── tests/            ← 测试（backend/tests pytest 256 个 test_*.py + frontend Jest 125 套/128 个 spec 文件）
└── perspectives/     ← 跨切专题（docs/roadmap/perspectives：调用链 / 约定 / 风险 / 测试覆盖）
```

## 功能域速查（第一层直达）

> 按功能词直达源文件，无需逐层翻页。定位时先在 `docs/roadmap/` 下 `Grep -i <功能词>` 命中本表行，再读对应源文件（路径相对 `frontend/src` / `backend/app`）。

| 功能域（含检索词） | 前端入口 | 后端入口 |
|------|---------|---------|
| 桌面双语 i18n language | `i18n/`（vue-i18n@8.28.2 单例 + zh-CN/en 消息树 P1 navigation/time/el + P2 auth/common/settings/downloader/errors + 语言解析/持久化/缺译回退 + apiErrorMessage 错误契约本地化）、`router.ts` 桌面路由 `titleKey`（移动路由不带保持中文）、`permission.ts` afterEach `resolvePageTitle`、`utils/formatters.ts` 相对时间、`layout/components/Navbar/index.vue` + `views/login/index.vue` 语言切换入口（自名常量）、`components/Breadcrumb` + `Sidebar/SidebarItem` `routeTitle()`、`store/modules/app.ts` SetLanguage；键集合/插值/复数一致性由 `tests/unit/i18n-message-parity.spec.ts` 门禁；选型与风险记录 PLANS/bilingual/p1-i18n-decision.md | 无（纯前端）+ P3-2 起含后端 preset_key 迁移 |；**2026-09-20 P6-5 收口**：i18n-leftover-guard 审计集由正向 65 面翻转为全桌面负向排除（排 mobile/layout-mobile/i18n/nested/tree/demo/*.generated.ts/__tests__，扫描器增行内 HTML 注释与行尾块注释剔除），白名单扩至 60 条（router title 数据身份/status-config label/advancedSearchFields label 兑底/条件组 API 载荷 T01）；遗漏扫描修补 ~40 条（404 联系支持 toast、ThemeSwitcher + theme.ts nameKey/descriptionKey、DemoModeBanner 四条、downloaderSettings store 九键、error-normalize 兑底、clipboard 两态、sync-task 四态通知、notification-markdown 兑底、traditionalStatusFilter 全部/活动中）；死代码删除 CompactTable.vue + types 死数组；新增 p6-c01-mobile-chinese-regression 15 例（C01 移动中文回归抽查）；R06 审校清单 PLANS/bilingual/orphan-files-review.md 待签认；**2026-09-21 P3-1**：语言包新增 search/torrent/dashboard + common 扩展 multiSelect/pageSize，契约链 labelEn（advanced_search_contract.json 38 操作符→生成器→generated.ts），共享层/列表骨架/添加弹窗/批量与删重反馈/仪表盘落地（详 entry/views/components-layout 分支）；**2026-09-21 P3-2**：新增 tracker/queryTemplate 模块 + search 扩 builder/workspace/valueInput/sizeRange/templateDialog/validation/presets + torrent 扩 detail，详情弹窗/Tracker 卡片与操作/汇报文案/高级搜索外壳/查询模板页落地；后端 P4 子范围提前：search_templates preset_key 迁移 b3e5f7a9c1d2 + init 按 key 幂等 + API 透出，预设名/描述/内置组名按 preset_key/稳定组 id 展示映射（详 backend infra/data-models 分支）；**2026-09-19 P5**：新增 recycleBin 模块 + torrent 扩 deleteLevel 子树（四级删除确认/结果按等级独立成键）+ common.listSeparator + errors.byCode 扩 14 键（TORRENT_DELETE_*/DOWNLOADER_UNSUPPORTED_TYPE/ADAPTER_INIT_FAILED/RECYCLE_BIN_*/NOT_IMPLEMENTED），删除链路收敛 mixin 单点 + 回收站页全量双语（详 entry/views 分支与 PLANS/bilingual/error-contract.md §3.2）
| 孤儿文件管理 orphan | `views/orphan-files/index.vue`、`api/orphan-files.ts`（后台扫描轮询；文件夹展开后懒加载/独立分页；仅可见文件实时统计硬链接；快捷操作+筛选区一键"已定位副本"筛选读预扫描结果；副本位置弹窗行级删除副本；Android 主服务端由 `orphan_files` 能力隐藏入口；✨2026-09-20 双语 P6-4b：全量 i18n（orphanFiles 模块，E01 拒绝/部分失败明细转 console）） | `api/endpoints/orphan_files.py`（✨2026-09-20 双语 P6-4b：失败路径 data.reasonCode 22 键）；`services/orphan_scan_job_service.py` / `orphan_file_service.py` / `orphan_scanner.py` / `orphan_purge_job_service.py`；`startup/lifecycle.py:304` Android 启动收敛历史任务；`tasks/scheduler/orphan_*_task.py` 能力门禁 |；2026-09-22 dev1.0.7 合入：i18n 取 dev 完成态（P6-5），MCP/MoviePilot 新面暂入审计排除清单待双语另立项 |
| 种子管理 torrent | `views/torrents/`（index.vue、TraditionalView.vue、`components/TrackerDetailCard.vue`、`mixins/errorTooltipDismiss.ts`）、`api/torrents.ts`；两视图按已同步 Tracker 主机域名筛选，快捷操作排查错误且全局同名同大小内容唯一的单种；新增“辅种数量”列，数量由种子信息同步任务计算，删除/转移后增量校正；Tracker 完整详情弹框（标题/关闭/页签/内容区）由共享组件统一，表格视觉由 `_tracker-table.scss` 统一；错误原因 tooltip 在滚轮/滚动时主动收起，列表查询使用全屏锁滚动蒙版；实时速度快照支持 200/206，携带 status/downloadComplete，完成证据强制进度 100，三种视图按 downloader_id+hash 收敛终态；运行态新复合键触发一次权威列表重拉，批量添加完成信号兜底首次刷新早于入库的竞态 | `api/endpoints/torrent_crud.py` / `torrents.py` / `torrents_async.py` / `torrent_deletion.py` / `torrent_status.py` / `torrent_location.py` / `torrent_speed.py` / `torrent_sync.py`；`getList` 支持 `tracker_domain` / `single_error_only`，域名来自 TrackerInfo 定时同步数据；Transmission 同步会持久化/清除错误原因，并把 Tracker announce/scrape 统计归一为 0–4 状态码，避免“已联系失败”误显示为“未联系”；列表名称 tooltip 与 Tracker 卡片展示 `errorReason`；同步统一走缓存下载器客户端、短事务与可续跑 cursor，并全局按 `name + size` 校正辅种数量；`torrent_speed.py` TTL 补查按下载器轮转并提供 `/runtime-state/reconcile` 复合键终态核验；`services/auxiliary_seed_count_service.py` / `deletion_task_manager.py` / `torrent_crud_service.py` / `torrent_batch_add_service.py` / `torrent_deletion_service.py` / `torrent_location_service.py` |
| 错误单种排查 single-error torrent | `views/torrents/index.vue` / `TraditionalView.vue`（快捷操作、Tracker 主机域名多选和可退出排查提示） | `torrent_crud.py:get_torrents` + `torrent_helpers.py:get_torrent_infos`（`single_error_only`：错误且全局同名同大小内容唯一；同一任务的多个 Tracker 服务不影响唯一性） |
| 下载器管理 downloader | `views/downloader/`、`views/mobile/downloader.vue`、`api/downloader.ts`；手动种子同步提交后由 `views/downloader/sync-task.ts` 轮询真实后台终态，桌面/移动端不再将受理误报为完成；列表启停开关只传 `id+enabled` 最小部分更新（2026-09-07，`upDownloader` 收紧为 `DownloaderUpdatePayload` 类型） | `api/endpoints/downloader*.py`、`api/endpoints/torrent_sync.py`；`core/background_task_manager.py` 原子防重、runner 强引用与业务结果终态映射；`services/downloader_adapters/` / `downloader_api_runtime.py` / `downloader_capabilities_manager.py` / `downloader_settings_manager.py` / `path_maintenance_service.py`；`models/downloader*.py`；`downloader/request.py` UpdateDownloader 的 is_search/is_ssl/enabled 缺省=不修改（case-when 部分更新，2026-09-07） |
| Tracker 管理 tracker | `views/tracker/`、`api/tracker.ts` | `api/endpoints/tracker*.py`；`services/reannounce_service.py` |
| 任务/定时任务 task cron | `views/tasks/index.vue`、`api/tasks.ts`（outcome/stale 展示 helper 由实例方法暴露给模板；查看日志保留可见任务筛选，清空后立即恢复全部日志） | `api/endpoints/tasks.py` / `cron_tasks.py`；`tasks/`（scheduler） |
| 审计日志 audit | `views/logs/audit.vue`、`api/audit-logs.ts`（✨2026-09-20 双语 P6-4b：全量 i18n，操作类型 19 项键化双形态） | `api/endpoints/audit_logs.py`（✨2026-09-20 双语 P6-4b：失败路径 data.reasonCode 7 键）；`services/audit_service.py` / `audit_service_sync.py` |
| 回收站 recycle | `views/recycle-bin/index.vue`（搜索区复用孤儿文件管理页 UI）、`api/recycle-bin.ts` | `api/endpoints/recycle_bin.py`；`services/recycle_bin_service.py` |
| 通知中心 notification | `layout/components/NotificationDrawer/`、`views/mobile/notifications.vue`（移动端顶部“全部已读”与未读摘要）、`api/notification.ts`、`store/modules/notification.ts` | `api/endpoints/notifications.py`；`services/notification_service.py`；`models/notification.py` |
| 查询模板 query-template | `views/query-templates/`（行操作为 Lucide 极简按钮） | `api/endpoints/advanced_search.py`；`services/advanced_search.py`；`models/search_template.py` |
| 标签管理 tag | 下载器页 TagManagementTab | `api/endpoints/tag_management.py`；`services/tag_service.py` / `tag_sync_service.py` / `tag_adapters/`；`models/torrent_tags.py` |
| 高级搜索 advanced-search | `components/torrents/AdvancedSearchWorkspace.vue`（左侧已保存搜索选择/创建/更新/删除）+ Builder（契约过滤操作符；下载器显示 nickname/提交稳定 ID；超级做种三态；包含/排除模式原样传输）+ 两种种子视图 | `api/endpoints/advanced_search.py`；`services/advanced_search.py`（20 字段；`error` 复用列表语义；Tracker 否定走 `NOT EXISTS`；文本字面匹配、标签完整 token、空值严格补集、回收站排除）/ `sqlite_search_runtime.py` |
| 种子转移 seed-transfer | — | `api/endpoints/seed_transfer.py`；`services/seed_transfer_service.py`（成功后立即落库目标行/源行 dr=1，并增量维护辅种数量，审计含 IP）；`models/seed_transfer_audit_log.py` |
| 重复种子/同内容排查 duplicate same-content inspection | `views/torrents/index.vue` / `TraditionalView.vue`（同 Hash 开关 + 快捷操作“辅种异常排查”；当前表格内筛选/排序/分页并可退出）+ `components/torrents/QuickDeleteDuplicatesDialog.vue` | `api/endpoints/duplicate_torrents.py`（同 Hash 重复查询）/ `duplicate_quick_delete.py`；`torrent_crud.py:get_torrents` + `torrent_helpers.py:get_torrent_infos`（`same_content_only`：同名同大小且不同规范化 Hash，复用列表分页；v1.0.6.40 起状态/Tracker 仅组内显示过滤，不参与成组判定）/ `services/duplicate_quick_delete_service.py` |
| 种子备份 torrent-backup | `views/torrents/FileManagement.vue`、`api/torrents.ts` / `api/torrents-backup.ts`（`torrent_backup` 能力门控） | `api/endpoints/torrent_backup.py`；`services/torrent_file_backup_manager.py`；`models/torrent_file_backup.py`；同步/恢复链路在 Android 跳过远端文件备份 |
| 仪表盘 dashboard | `views/dashboard/index.vue`、`api/dashboard.ts` | `api/endpoints/dashboard.py`；`services/dashboard_service.py` |
| 速度计划/设置 speed-schedule | `views/settings/index.vue`（含"状态诊断"页签：一键导出故障转储/状态分析 JSON，demo 模式生成前端本地快照）、`api/health.ts` | `api/endpoints/downloader_settings.py`；`services/speed_schedule_service.py`；`api/endpoints/health.py`（诊断导出） |
| 静态展示 Demo demo | `src/demo/`、`src/components/common/DemoModeBanner.vue`、`package.json:build:demo` | —（Demo 请求层不访问后端） |
| MoviePilot 整理联动 moviepilot ✨2026-09-09 | `views/settings/components/MoviePilotPanel.vue`（设置页 MoviePilot 页签：全局开关 CAS/实例与下载器映射/路径反查）、`views/torrents/mixins/detailTabsData.ts` + `components/TrackerDetailCard.vue`（种子详情「媒体库」页签）、`api/moviepilot.ts`；插件源在独立仓库 [MoviePilot-Plugins-BtDeck](https://github.com/strainhzj/MoviePilot-Plugins-BtDeck)（2026-09-10 发布，自本仓库根 `moviepilot-plugin/` 拆出） | `api/endpoints/moviepilot.py`（`/api/v1/moviepilot/*`：握手/同步/设置/实例/关联正反查）；`services/moviepilot_integration_service.py` / `moviepilot_settings_service.py`；`models/moviepilot_instance.py` / `moviepilot_transfer_history.py`；迁移 `053003337878`；凭据复用用户令牌体系（principal 门禁） |
| MCP 服务与外部 agent 对接 mcp ✨2026-09-22（W0~W4 合入 + W5 服务密钥） | `views/settings/components/McpSettingsPanel.vue`（设置页 MCP 页签：全局/能力开关 + kill switch 横幅 + 服务密钥卡——端点/认证头提示、三态引导、掩码复制、危险确认刷新）、`api/mcp-settings.ts`、`i18n/locales/{zh-CN,en}/mcp.ts`（面板/风险/服务密钥双语） | `api/endpoints/mcp_settings.py`（`/mcp/settings` CAS + `/mcp/apikey[/rotate]` 服务密钥控制面）；`services/mcp_apikey_service.py`（W5：哈希=认证事实源 + SM4 副本=查看出口 + revision CAS）；`mcp/`（contracts/catalog/runtime/server/auth/redaction/errors/tools，同进程挂载 `/mcp/`；transport 双认证路径：Web JWT 与 `btdmcp_` 服务密钥，`AUTH_API_KEY_INVALID` 稳定码） |

---

## 分支说明

| 分支 | 一句话职责 | 链接 |
|------|-----------|------|
| **backend** | FastAPI 后端总览与跨分支依赖骨架 | [backend/README.md](./backend/README.md) |
| ↳ app-root | `backend/app/` 包根 8 文件：应用工厂、DB 引擎、异常处理、配置入口、版本、桌面/WebSocket main | [backend/app-root.md](./backend/app-root.md) |
| ↳ desktop-companion | 桌面伴侣 profile、LAN 策略、健康检查、Windows DPAPI 凭据与 pywebview 会话恢复 | [backend/desktop-companion.md](./backend/desktop-companion.md) |
| ↳ api | HTTP 路由层（39 个 endpoints + schemas + models + responseVO） | [backend/api/README.md](./backend/api/README.md) |
| ↳ services | 业务服务层 + downloader_adapters + tag_adapters | [backend/services/README.md](./backend/services/README.md) |
| ↳ core | 基础设施（config/path_mapping/file_ops/tracker_*），⚠ 含 4 个 0 引用孤儿文件 | [backend/core/README.md](./backend/core/README.md) |
| ↳ contracts ✨v1.0.6.27 | 前后端共享机器可读契约（advanced_search JSON + Python 加载器，单一真相源） | [backend/contracts/README.md](./backend/contracts/README.md) |
| ↳ data-models | ORM 模型 + repositories + schemas + enums + 默认数据种子 | [backend/data-models/README.md](./backend/data-models/README.md) |
| ↳ tasks | 定时任务（cron）+ scheduler + 后台任务管理 | [backend/tasks/README.md](./backend/tasks/README.md) |
| ↳ domain | 领域目录：downloader / torrents / tracker / auth / user | [backend/domain/README.md](./backend/domain/README.md) |
| ↳ infra | utils（audit_logger/encryption/log_sanitizer/connectivity）+ startup + migrations + alembic | [backend/infra/README.md](./backend/infra/README.md) |
| **android** ✨2026-08-23 | 伴侣模式 App（Kotlin；不含 Python，服务端壳是 Phase 3） | [android-companion.md](./android-companion.md) / [../android/README.md](../../android/README.md) |
| **frontend** | Vue 2 + TypeScript 前端总览与分支索引；含独立静态 Demo 构建/请求分流 | [frontend/README.md](./frontend/README.md) |
| ↳ entry | 应用入口：Vue 实例化 / 路由 / 路由守卫 / 根组件 | [frontend/entry/README.md](./frontend/entry/README.md) |
| ↳ api | axios 封装的 12 个领域 API 模块 | [frontend/api/README.md](./frontend/api/README.md) |
| ↳ views | 13 个页面视图模块（⚠ class-component 与 Options API 并存） | [frontend/views/README.md](./frontend/views/README.md) |
| ↳ store | Vuex store（空壳 index + 5 个自注册 module） | [frontend/store/README.md](./frontend/store/README.md) |
| ↳ components-layout | 通用组件（AppLogo/Pagination/Breadcrumb/ThemeSwitcher/LucideIcon/PageSizeCombobox…）+ layout 骨架 | [frontend/components-layout/README.md](./frontend/components-layout/README.md) |
| ↳ demo | 静态展示 Demo：fixture / 内存状态 / request 分流 / 路由矩阵 | [frontend/demo/README.md](./frontend/demo/README.md) |
| ↳ utils-types | utils / types / constants / directive | [frontend/utils-types/README.md](./frontend/utils-types/README.md) |
| **deploy** | 多部署模式分叉：Docker Compose / PyInstaller 单机包 / Inno Setup / Android APK / fpm；含根目录统一构建入口 | [deploy/README.md](./deploy/README.md) |
| **tests** | 后端 pytest（256 个 test_*.py，按子目录组织）+ 前端 Jest（125 套，spec 文件 128 个：unit 113 + 组件内嵌 12 + e2e/mobile 4） | [tests/README.md](./tests/README.md) |
| **perspectives** | 跨切专题索引（架构调用链 / 约定 / 风险 / 测试覆盖） | [perspectives/README.md](./perspectives/README.md) |

---

## 阅读层级

- **第一层（本文件）**：模块路由，只看分支职责与导航。
- **第二层（各分支 README.md）**：该分支的文件清单表（文件名 / 行数 / 一句话职责 / 链接）+ 依赖关系。
- **第三层（源文件 .md）**：单个源文件的类/函数索引与方法签名详情（固定四节模板）。
  - 本次已完成样例：[backend/api/endpoints/torrent_crud.md](./backend/api/endpoints/torrent_crud.md)
  - 其余源文件第三层待后续按"模式 B（补充）"增量补齐。

---

## 相关文档（双向链接 ⇄）

| 文档 | 关系 | 路径 |
|------|------|------|
| 项目 README | ⇄ 快速开始 / 技术栈总览 | [../../README.md](../../README.md) |
| 根 CLAUDE.md | ⇄ 全栈协同约束 | [../../CLAUDE.md](../../CLAUDE.md) |
| 根 AGENTS.md | ⇄ 全栈工作流路由层 | [../../AGENTS.md](../../AGENTS.md) |
| 后端 CLAUDE.md | ⇄ 后端技术约束 | [../../backend/CLAUDE.md](../../backend/CLAUDE.md) |
| 前端 CLAUDE.md | ⇄ 前端技术约束 | [../../frontend/CLAUDE.md](../../frontend/CLAUDE.md) |
| 架构深度分析 | 调用链详述来源（配置/迁移双轨、定时任务隔离） | [../../backend/docs/architecture-deep-dive.md](../../backend/docs/architecture-deep-dive.md) |
| 架构独立审查 | 对深度分析的 review | [../../backend/docs/architecture-review.md](../../backend/docs/architecture-review.md) |
| 后端约束集 | API 响应格式 / 数据库迁移 / 下载器连接等 6 条 | [../../backend/docs/constraints/](../../backend/docs/constraints/) |
| 前端约束集 | Options API / 公共变量 / 异步上下文等 6 条 | [../../frontend/docs/constraints/](../../frontend/docs/constraints/) |

> 路线图遵循"单一真相"原则：架构论述、约束条款已在上述文档中存在的，本路线图只放**索引 + 链接**，不复制全文。详见 [perspectives/README.md](./perspectives/README.md)。

---

## 元信息

| 项目 | 值 |
|------|-----|
| 本次维护 | 2026-09-10：手机端七问题修复批次（mobile-ux-fixes，详见 feature_list 同名任务组）；2026-09-08：移动端通知一键已读 |
| 生成日期 | 2026-09-22（W5 MCP 服务密钥批：新增 `services/mcp_apikey_service.py`（services 54→55）+ `tests/api/test_mcp_apikey.py`（api 76→79）；mcp_settings 端点/principal 内核/audit_enums（48→55 成员）/McpSettingsPanel/mcp-settings API/demo 层/i18n mcp 模块各内容行更新；测试总数实测校准 236→256 与 Jest 124→125 套；功能域速补 MCP 行） / 2026-09-21（roadmap 全量同步批：双向审计全部分支 README 与实际源码——补录 9 缺失文件行 + mobile/ 子树 15 行，划线 1 陈旧行，刷新计数 12 处；第三层 2 样例行号全量重测（torrent_crud 727→485 行重构后贴合、orphan_file_service 3902→3911 行分段偏移修正）；测试数量重测（后端 236 test_*.py、前端 124 套/128 spec）） / 2026-09-20（桌面双语 P6-5 收口批：六批遗漏扫描修补 ~40 条 + 审计门禁翻转为全桌面负向排除 + 死代码删除（CompactTable.vue 零消费方 + types 死数组）+ C01 移动中文回归抽查 spec 15 例 + R06 审校清单产出（orphan-files-review.md 待签认）；Jest 124 套 1814 例全绿） / 2026-09-20（桌面双语 P6-4b 审计日志/孤儿文件域批：views/logs/audit.vue + views/orphan-files/index.vue 全量双语；新增 auditLogs/orphanFiles 两语言包模块（操作类型 19 项短/长双形态、危险链路三要素、快捷操作 strong 分片、扫描状态三态）；后端 audit_logs.py 7 键 + orphan_files.py 全端点 22 键 data.reasonCode + 动态 str(e)/scan_id msg 固定化；E01 拒绝/部分失败明细转 console（对齐回收站 P5 先例）；审计集扩至 65 面；Jest 123 套 1799 例、后端全量 4832 例全绿） / 2026-09-20（桌面双语 P6-4a 任务域批：views/tasks 任务页全量双语（双页签/表单/三弹窗/批量与危险确认链路）+ CronEditor/MonacoEditor/PythonClassSelector 三组件 + api/tasks 共享展示层（outcome 六态 + stale tooltip，移动端复用零回归）；新增 tasks 语言包模块（tabs/list/type/status/outcome/stale/logs/form/syntax/logDetail/preview/cleanupDialog/msg/dialog/cronEditor/monaco/pythonSelector ~330 键）；后端 cron_tasks 全端点失败路径 data.reasonCode 29 新键（TASKS_*）+ 动态 str(e) msg 固定化 + CRUD 冲突明细只进日志；PythonClassSelector 预定义类改由后端 type-config pythonClasses 驱动（getTaskTypeConfig 零调用→接线，硬编码假类树删除）；CronEditor 内置模板身份 key 化（中文 name 匹配移除）；审计集扩至 63 面 + 数据语义白名单 38 条；Jest 122 套 1777 例、后端全量 4796 例全绿） / 2026-09-20（桌面双语 P6-3 Tracker 管理域收尾批：views/tracker 全域 4 页面 + 9 组件 + utils/tracker.ts 全量双语（关键词看板/搜索/汇报配置/判断测试 ~500 条）+ tracker 语言包新增 pools/board/search/reannounce/testTool/addDialog/importDialog/listModal/quickAction/keywordCard/timeline/resultSummary/apiLog/lang/errors 十五子树；后端 tracker_keywords/keywords_pools/reannounce/test 四端点失败路径 data.reasonCode 18 新键 + 动态 str(e) msg 固定化 + reannounce not-found 判定改结构化 error_code（禁「不存在」中文子串匹配）；errors.byCode 扩 18 键；审计集扩至 58 面；utils/tracker 死代码清理（7 个零消费方导出）+ poolLabel/poolOptions 共享池名；3 套件迁移 i18n 挂载；Jest 121 套 1781 例、后端全量 4776 例全绿） / 2026-09-19（桌面双语 P6-2 下载器域收尾批：8 组件 ~300 条全量双语（设置弹窗页签骨架 + 速度/高级/路径管理/路径映射/路径维护/标签/模板）+ downloader 模块八个子树 + 键化数据数组（星期/高级字段/映射类型）；后端设置模板 preset_key 迁移 d1e2f3a4b5c6（5 冻结键+P0 方案，含 UNIQUE 约束差异导致的占名分流）+ 前端 template-presets 展示映射；审计集 44 面，Jest 120 套 1729 例）/ 2026-09-19（桌面双语 P6-1 种子域收尾批：传统视图 136 条 + 转移/批量转移/修改路径弹窗 + 全局替换 Tracker 弹窗 + 文件管理页全量双语；新增 transfer/fileManagement 模块 + tracker.replace 子树 + torrent 传统视图键；状态筛选改 localizedStatusOptions、列设置 labelKey、四级删除菜单与确认框同键；新增键可达性门禁（全仓 1280 键）当场抓出并修复 Navbar 8 处错前缀与 SizeRangeFilter 键路径两个真实空串缺陷；Jest 119 套 1704 例）/ 2026-09-19（桌面双语遗留清扫批：对 P1～P5 已声明完成的 29 个桌面面机器审计，修复 8 处用户可见漏译——Sidebar 移动版入口/aria、PWA RefreshPrompt、AdvancedMultiSelect 默认占位、PlatformCapabilityPanel 整面板、downloader 控制台反馈 17 条、formatters 错误提取（HTTP 表/网络/未知/上下文）、高级搜索请求校验 22 条 + assertSameDownloader、user store 抛出 4 条；新增 tests/unit/i18n-leftover-guard（19 例：审计门禁+白名单腐化守卫+双语行为契约），全量 Jest 118 套 1687 例）/ 2026-09-19（桌面双语 P5 代码批：四级删除与回收站——后端 torrent_deletion/recycle_bin 全端点 data.reasonCode 14 新键（E13～E16，E14 双形态 code=200 冻结）+ 动态 msg 收敛 11 处；前端删除链路收敛 mixin 单点（index.vue 本地重复链路 ~340 行删除 + legacy 双问删除 ~570 行死代码清理）+ deleteLevel 子树 45 键按等级独立成键 + 回收站页全量双语（recycleBin 模块 78 键）；errors.byCode 累计 72 键；新 backend test_reason_contract_p5 18 例 + frontend 契约用例，全量 Jest 117 套 1668 例）/ 2026-09-21（桌面双语 P4 剩余子范围：错误契约 M1 扩展——后端 torrent_status/tracker/advanced_search 模板 CRUD/torrent_add_service/add-batch/sync 失败路径 data.reasonCode 共 33 键 + 动态 str(e) msg 固定化、E17 前端 422 按 type/loc 字典化 + apiResponseMessage；E03 通知事件双语——NotificationDrawer/Item 按 extra_data.event 本地化四事件 + 后端 version_update event 键/orphan_count_warning 标志；新测试 backend test_reason_contract_p4 30 例 + frontend notification-display/api-error-message-p4 36 例 + drawer 详情扩 4 例，全量 Jest 117 套 1660 例）/ 2026-09-21（桌面双语 P3-2 第二批：详情/Tracker 卡片与操作/汇报文案/高级搜索外壳/查询模板页与 4 预设展示 + 后端 preset_key 迁移提前；Jest 115 套 1620 例全绿）/ 2026-09-21（桌面双语 P3-1 第一批：契约链 labelEn + 共享层稳定值双语 + 种子列表骨架/添加弹窗/批量与删重反馈/仪表盘；Jest 115 套 1617 例全绿）/ 2026-09-18（桌面双语 P2 第一批：首次使用闭环文案双语化——auth 组/设置 2FA·改密·诊断/下载器控制台·卡片·设置弹窗 basic 页签/通知壳层；错误契约 M1 子集——后端 login/cuser/downloader data.reasonCode + 前端 apiErrorMessage 本地化 + 动态 str(e) 收敛固定文案；新 spec 契约 11 例 + 前端 setup 钉 zh-CN） / 2026-09-18（桌面双语 P1 基础设施：vue-i18n@8.28.2 精确钉版安装 + `src/i18n/` 四文件（types/index/element-locale.d.ts/locales zh-CN·en navigation+time）+ 21 处桌面路由 titleKey + Element Locale 挂接 + permission 标题走 resolvePageTitle + formatters 相对时间 i18n + Navbar/登录页语言切换入口 + app store SetLanguage；entry 分支行号全量重测） / 2026-09-10（mobile-ux-fixes 手机端七问题批次：nginx /health 代理修复+双端健康探测回退 /api/v1 别名、MessageBox 手机适配、幂等 GET 瞬态重试、tracker-domains TTL 缓存+懒加载+with_trackers 瘦身、移动种子页快捷操作/查重排查模式、桌面版入口全移除、下载器新增/编辑统一 DownloaderSettingsDialog+≤780 顶部页签+旧 DownloaderDialog 删除） 2026-09-07（/health/sync 改造为 /health/diagnosis 故障转储导出端点 + 下载器更新接口部分更新契约修复） / 2026-09-06（移动端高级搜索移动原生重构：方案三——MobileAdvancedSearch 摘要卡构建器+ConditionEditSheet 底部弹层+advancedSearchFields 桌面/移动共享层） / 2026-09-06（批量添加种子偶发 database is locked 根修：`torrent_batch_add_service` 陈旧读快照治理——迭代顶部结束遗留读事务 + BUSY/BUSY_SNAPSHOT 有界重试 + sqlite_errorcode 观测透传） / 2026-09-06（第二批：OOM 治理外部审查四缺陷修复——M_PURGE 符号/摘要全程有界/全量标记时序+原子写/脚本取消清理） / 2026-09-06（TrackerDetailCard 底部收起条+文件/Peers 页签：后端 torrent_detail 端点+前端数据 mixin+卡片页签实现） / 2026-09-06（桌面终态整表刷新循环根修：转移判定+复合键去重+高级搜索门控） / 2026-09-05（移动端验收修复二轮：v-infinite-scroll 自动加载失控根修 WindowInfiniteScroll） / 2026-09-05（移动端验收修复：下拉刷新滚动容器误判/移动四级删除补齐/刷新塌陷与终态循环治理） / 2026-09-05（定时任务 OOM 峰值治理批次：reannounce 预过滤重写/qB-TR 同步分批化/cron 输出上限/RSS 采样） / 2026-09-04（2FA 降级/契约 CRLF 规范化/桌面回环豁免/Android 钉扎与 e2e 布局修复） / 2026-07-25（首次）/ 2026-07-30（增量更新：v1.0.6.25~32）/ 2026-08-04（增量更新：v1.0.6.33~36）/ 2026-08-06（增量更新：v1.0.6.37）/ 2026-08-09（异步操作占用）/ 2026-08-11（同步阻塞修复、孤儿硬链接与 UI/重复查询修复）/ 2026-08-12（种子文件、任务日志、高级搜索、错误原因及 Tracker 判断修复）/ 2026-08-13（孤儿扫描 12 万级后台化）/ 2026-08-14（大库迁移中断恢复、启动 fail-fast、孤儿文件页面视图模式与嵌套表头修复、Tracker 主域名筛选与错误单种排查及卡片样式统一）/ 2026-08-15（种子文件备份补偿、孤儿副本整体定位与筛选下拉提示语；同日副本定位改为定时预扫描落库；第三批：located 副本筛选 + 预扫描范围收紧）/ 2026-08-16（副本位置弹窗行级删除副本）/ 2026-08-16（第二批：进度精度舍入、转移后立即落库目标行、手动操作审计补 IP）/ 2026-08-17（双密钥会话过期登出与重登录生效修复） / 2026-08-18（W9 强制改密路由死锁修复）/ 2026-08-18（第二批：同内容排查状态/Tracker 改组内显示筛选）/ 2026-08-18（第三批：跨标签令牌续期竞态修复——三态续期/ExpireSession 保留 refresh cookie/守卫网络错误分流/后端原子轮换）/ 2026-08-18（第四批：令牌机制对抗审计修复——升级密钥补齐/业务 401 改码/refresh_tokens 清理任务/SECRET_KEY YAML 持久化/跨标签级联根修/5xx 瞬时逃生/审计下载 blob/网络 toast 节流/el-upload 401 引导）/ 2026-08-20（第二批：展示对齐判定——Tracker 异常可见化与 Announce 状态覆写）/ 2026-08-23（安卓适配 Phase 1：统一 TCP probe 与依赖瘦身）/ 2026-08-23（第二批：Phase 2 伴侣模式 MVP 脚手架 + health version 字段）/ 2026-08-26（`BtDeck` 效果图字标与 App 微型图标）/ 2026-08-27（种子错误 tooltip 滚动收起与查询全屏锁定蒙版）/ 2026-08-27（第四批：Windows EXE/安装包品牌图标）/ 2026-08-27（EXE/APK 构建脚本）/ 2026-08-28（移动端 UX 增强）/ 2026-08-29（种子实时终态收敛与部分快照容错）/ 2026-08-30（种子列表成员自愈与批量添加完成刷新）/ 2026-09-03（发布门禁批次 D/E/F：scripts/release 签名+清单+汇聚+注入演练工具链、torrents_async 3.11 取消竞态生产修复）/ 2026-09-05（MCP 前置 service 层解耦：runtime_context/audit_context/principal/torrent_add_service/cron_trigger 五新模块 + Dashboard/等级4删除/添加端点去 Request 化）|
| 2026-08-20 增量 | 新增 `auxiliary_seed_count` 数据链路、种子信息同步全量校正、删除/转移/还原增量维护，以及两种种子列表视图字段；同步 Alembic head `975dad435c03` 与相关测试入口 ；2026-09-22（dev1.0.7→dev 合并批：迁移链重挂单 HEAD 053003337878、功能域/子树行合并 MCP+MoviePilot、主机能力行删除、计数待全量重测） |
| 2026-08-20 增量（第二批） | 展示对齐判定：新增 `core/tracker_keyword_map.py` 共享关键词池加载器（判定任务 `_load_keywords` 委托复用）；`tracker_status_policy.py` 新增 `tracker_message_failed`/`tracker_display_failed`（与判定任务中性码语义一致）；`torrent_helpers.py`/`duplicate_torrents.py` announce/scrape 展示文本按失败池覆写 + VO 透传 `has_tracker_error` + duplicates error 筛选口径对齐（OR has_tracker_error）；前端 `torrentBatch.ts` 新增 hasTrackerError/showTrackerErrorTag/getTorrentErrorReason 共享 helper，两视图状态列叠加红色 Tracker异常 标签 |
| 2026-08-21 增量 | 任务日志与孤儿文件页统计摘要接入全局 `CollapsiblePanel`，分别以 `btdeck_task_log_stats_collapsed` / `btdeck_orphan_file_stats_collapsed` 持久化折叠状态；前端管理页契约测试扩展为 14 项 |
| 2026-08-22 增量 | roadmap 全量对账刷新（基准 HEAD 348c700）：补记 04c8ec6 mypy 清零/ORM Mapped 迁移批次（143 个后端文件行号整体漂移）；汇总计数实测重校（endpoints 37、alembic 28 个 revision/head `975dad435c03`、后端测试 180、前端 spec 59、api 模块 12、store 4+1 拆分）；清理 7 条失效条目、补录 29 个漏列文件（含 5 个新 revision）；第三层两文档行为描述重写（批种添加 202 后台化、孤儿副本数快照列、`call_downloader_api` 统一下载器调用） |
| 2026-08-23 增量（同步资源观测） | `resource_guard` 记录 heavy_sync holder 的 task/run/phase/进程信息并补充 wait_timeout 诊断；`cron_executor` 增加 Python 内部类生命周期心跳/超时告警；`sync_coordinator` 暴露阶段耗时与最近进度；新增 92 项针对性回归覆盖中的资源 holder 与生命周期观测用例 |
| 2026-08-23 增量（tracker_sync 异常边界观测） | `tracker_sync_task`、`sync_coordinator` 与 qB/TR tracker-only 路径补充下载器阶段、异常类型、traceback、错误计数和 `event=sync_error`（含是否继续执行）；有错误的结果汇总提升为 warning；新增 82 项定向回归覆盖 |
| 2026-08-25 增量（定时任务停止治理） | cron_executor 超时强制终止（`CRON_TASK_TIMEOUT_ENFORCE` 默认开，`TaskExecutionTimeoutError` 穿透兜底、任务体 TimeoutError 以 `_TaskBodyTimeoutError` 包装区分，timeout<=0 归一不强制）；interrupt 真取消（协程句柄自登记 + cancel 等收尾 + `outcome=cancelled`/success=True 落库）；`update_task_freshness` MissingGreenlet 根修（`onupdate=func.now()` postfetch 过期 → commit 后 `db.refresh`）；心跳停滞告警（`SYNC_TASK_PROGRESS_STALL_WARNING_SECONDS`，progress_stalled 入白名单）+ faulthandler 全线程栈自动转储；认证检查一次性 qb/tr 客户端补 requests 超时；interrupt 端点补审计（`SCHEDULED_TASK_INTERRUPT` 枚举 + 前端 audit.vue 映射）。测试：TestTimeoutEnforcement/TestInterruptRunningInstance/freshness 真实会话共 9 新例，相关套件 461 passed |
| 2026-08-23 增量（孤儿 Schema 漂移自愈） | 新增 Alembic 修复迁移 `c1d2e3f4a5b6`：针对版本号已到 `975dad435c03` 但 `orphan_current_candidate.current_detail_id` 缺失的存量库，后端重启自动补列/回填/索引；健康库幂等 no-op；新增迁移回归测试 |
| 2026-09-07 增量（Android 主服务端路径能力硬禁用） | `core/platform_capabilities.py` 新增下载器文件系统/路径映射/孤儿/种子备份/种子转移/三级删除六项能力与 schemaVersion=2；API/服务/任务/启动恢复统一门禁；`startup/lifecycle.py:304` 收敛历史孤儿任务且不触碰文件系统；前端路由/菜单/下载器路径配置/转移/三级删除/任务操作按矩阵隐藏或禁用 |
| 来源 | 首次新建（`docs/roadmap/` 此前不存在）；后续按源码变更增量同步 |
| 最近同步 | 2026-09-07：Android 主服务端文件系统能力硬禁用（能力矩阵/任务门禁/启动恢复/前端入口）；行号已按当前源码复测 |
| 分析范围 | backend/app/* + frontend/src/* + deploy + tests（全栈） |
| 2026-08-26 增量 | 前端 Logo 按效果图收口：完整横版改为绿色 D 形轨道、三条深色甲板线与 `Bt` 绿/`Deck` 深色字标，移除实验编号/中文说明；移动头部及 PWA/Apple/Android/favicons 全部使用绿色底反白 `micro` 光学版，生成脚本同步输出横版 PNG |
| 2026-08-26 增量 | 伴侣凭据记忆：Android `CredentialVault`（Keystore AES-GCM）与桌面 `desktop_companion/credentials.py`（Windows DPAPI），profile 增加 username；WebView/pywebview 切换时先隔离旧 cookie，再用一次性同源登录恢复 access/refresh token；新增 Android/桌面回归覆盖。 |
| 2026-08-27 增量 | 移动端三项调整：①移动查询模板页 `/m/query-templates` 裁撤（仅保留高级搜索，深链 redirect 至 `/m/search`，`m2-template-cache.ts` 与种子页/搜索页模板回填链路移除）；②高级搜索条件组移动适配：`AdvancedSearchBuilder.vue` 内联定宽全部类化 + 768px 断点强化（选择器铺满/组头换行/AND/OR 标签避让/对话框窄屏压宽），`ConditionValueInput.vue` 日期范围窄屏弹性对分；③新增 `/m/settings` 移动设置页（整页复用桌面设置组件），守卫强制改密落点/放行白名单按 UI 模式分流（移动落 `/m/settings`），`toMobilePath('/settings')` 映射补齐；entry 分支路由表与守卫行号全量重测 |
| 2026-08-27 增量（第二批） | Tracker 域名筛选命中可视化：`torrent_helpers.py` 保留 EXISTS/ANY 语义，入口统一归一域名后 SQL（like `escape` 字面量化）与 Python 谓词 `tracker_row_matches_domains` 同口径过滤，VO `tracker_info[].matched_domain` 标记命中行；`tracker_like` 空结果改返回空列表；前端 TrackerDetailCard 命中行高亮+「命中筛选」标签（`_tracker-table.scss`）、两视图 `[tracker-filter]` 观察日志（共享 `countMatchedTrackerRows`）、后端 `[tracker-domain-filter]`/`[tracker-filter]`/`[torrent-list]` debug 锚点；查询模板 simple 表单补 Tracker 域名多选（修复保存丢失 tracker 筛选）；torrents 视图分支与 torrent_crud 三层 md 同步 |
| 2026-08-27 增量（第三批） | 两种桌面种子视图接入 `mixins/errorTooltipDismiss.ts`：错误原因 tooltip 禁止进入浮层，并在捕获阶段滚动/滚轮时调用 `hide()` 收起；`listLoading` 改用 `v-loading.fullscreen.lock` 覆盖整个视口并锁定页面滚动；回归加固为 mixin/真实 Element UI Tooltip、真实 Loading DOM 行为、双视图 directive binding + 异常复位三层保护，前端 Jest 实测 84 suites / 1169 tests。 |
| 2026-08-27 增量（第四批） | Windows 发行图标修复：`generate-pwa-icons.py` 将品牌 `micro` mark 输出为 16~256px 多尺寸 ICO；`btdeck-windows.spec` 显式嵌入该 ICO，使 EXE 与 pywebview 运行窗口/任务栏使用项目 Logo；`btdeck.iss` 安装器、卸载项及三类快捷方式统一复用；打包契约新增 3 项回归。 |
| 2026-08-27 增量（构建脚本） | 新增 `deploy/build-android.bat`：相对仓库路径解析、Gradle/JDK/SDK 检查、严格/LAN 双 debug APK 构建、JVM 单测、产物复制与 `apksigner`/`aapt2` 校验；新增根 `build-packages.bat` 统一调用既有 Windows EXE 链和 Android 链，并支持按目标/变体选择。 |
| 2026-08-28 增量（移动端 UX 增强） | `views/torrents/mixins/speedPolling.ts` 增量扩展 `speedPollIntervalMs`/`startSpeedPolling(immediate)`（桌面默认行为零变化，移动端复用为通用 visibility 门控轮询）；`layout/mobile/index.vue` 四 Tab 加 LucideIcon（house/hard-drive/download/bell）、二级页 header ←（固定回退映射 replace：详情→种子/设置→下载器/关键词搜索→看板/其余→仪表盘）与汉堡并存、标题用 meta.title、汉堡触控区 44×44、60s 未读轮询 document.hidden 门控；`views/mobile/torrents.vue` 10s 速度轮询（复用 torrentBatch `buildSpeedSnapshot` + traditionalTorrentIdentity 精确合并，ready 未命中行清零防冻结）、卡片速度行（>0 才渲染+min-width 防抖）、`v-infinite-scroll` 无限滚动+尾部计数、返回顶部浮标、暂停/恢复乐观状态更新、空态 CTA（无下载器→`/m/downloader?create=1`，downloader.vue mounted 解析直达新增弹窗）；`dashboard.vue` 15s/`notifications.vue` 30s 静默自动刷新（silent 不置 loading）、通知按 id 去重分页追加+静默刷新翻页互斥（已翻页只同步未读角标）、下载器空态 CTA、两页移除 m-refresh；`torrent-detail.vue` 手写 setInterval 迁移 SpeedPollingMixin（5s+后台暂停）并移除底部冗余返回排；`tasks.vue` 删除按钮 margin-left:auto 分隔；`pull-to-refresh.ts` 手势内滚动回顶重置起点防指示条跳变。测试：speed-polling 9 / mobile-shell 31 / mobile-torrents 21 / mobile-notifications 17 / mobile-dashboard 15 / mobile-torrent-detail 7 / mobile-downloader 11 / pull-to-refresh 9，前端全量 84 suites / 1216 tests；e2e mobile-interactions 修正查询模板存量失效断言并新增二级页返回/Tab 图标用例；提交后验证抓出返回顶部两处缺陷（滚动容器实为 window 而非 .mobile-content；箭头函数类字段 this 指向 vue-class-component 收集后丢弃的实例致数据写入静默失效）并改为 window 监听 + prototype 方法。 |
| 2026-08-29 增量（种子实时终态收敛） | `backend/app/api/endpoints/torrent_speed.py` 补充 qB/Transmission 状态与 `downloadComplete`，完成态强制进度 100 并同步数据库；TTL 补查按下载器轮转、退避且确认完成后移除；新增 `/torrents/runtime-state/reconcile` 复合键终态核验。`frontend/src/views/torrents/utils/torrentBatch.ts` 支持 206 部分快照、终态归一和连续未命中候选；列表、传统、移动三视图按 `downloader_id + hash` 更新并在核验后刷新状态筛选。 |
| 2026-08-29 增量（桌面折叠侧栏 Lucide） | `SidebarItem.vue` 将折叠态从广泛隐藏 `el-submenu__title` 直接子 `span` 改为仅隐藏 `.submenu-label`/`.submenu-chevron`，显式保留 Lucide `.menu-icon`；修复种子管理与 Tracker 管理因多子菜单进入 `el-submenu` 分支后父图标消失。新增真实组件 + 实际 SCSS 回归 `sidebar-collapse-lucide.spec.ts`。 |
| 2026-08-30 增量（种子列表成员自愈） | `RuntimeListMembershipTracker` 以首个完整活动快照建立分页外基线，随后仅对新出现且未展示的 `downloader_id + hash` 触发串行权威列表刷新，并立即重放同轮速度；206 仅增量合并基线。`TorrentAddDialog.vue` 在 202 返回后按 `task_id` 轮询既有完成通知，列表/传统视图收到 `batch-complete` 后再次拉表并补一次速度；覆盖首次刷新早于数据库写入，以及零速度、暂停或瞬间完成无法由活动快照发现的新增种子。 |
| 2026-08-30 增量（下载器手动同步异步生命周期） | `POST /torrents/sync-single` 以原子 pending/running 占用立即返回 `task_id`，`BackgroundTaskManager` 保留 runner 强引用并将结构化 failed/cancelled 结果映射为真实终态；仍经 `SyncCoordinator(full/manual)` 且只使用 `app.state.store` 缓存客户端。桌面与移动下载器页共用 `sync-task.ts` 轮询 `/sync-status/{task_id}`，仅在 success/failed/cancelled 终态释放同步态并呈现真实结果。 |
| 2026-09-05 增量（移动端验收修复二轮：无限加载失控） | 用户复验"还在不断触发刷新按钮"后真栈复现（Playwright CDP 触摸仿真 + 8081 dev + 5001 真后端，dev 库 22437 种子）：**空闲 65 秒 74 次 getList**，页面打开即自动连发直到拉满 total。根因：Element v-infinite-scroll 把布局壳 `.mobile-content`（overflow-y:auto）选为滚动容器，但 `.mobile-layout` min-height:100vh 布局下长内容把布局撑高、`.mobile-content` 从不内滚（高度恒等于内容）——指令"列表底部距容器底部"几何恒真，且 immediate 的 MutationObserver 把每次 DOM 变化（轮询速度更新/追加卡片本身）都变成一次 loadMore。demo 桩 9 条种子瞬时返回从未暴露（一轮修复的 demo 探针空闲 0 次是真阴性遮蔽）。修复：新增 `views/mobile/mixins/window-infinite-scroll.ts`（window scroll + isNearViewportBottom/maybeLoadMore、短内容页加载后主动补页、原型方法防 vue-class-component 箭头字段 this 坑），torrents.vue/notifications.vue 移除指令接入 mixin。复测对照：初始 2 页 40 条（补页至视口溢出即停）、空闲 65s **0 次**、中部手势 0 触发、滚到底 1 页追加、顶部下拉 1 次。测试：window-infinite-scroll 7（新）/mobile-torrents 37/mobile-notifications 20，全量 101 suites/1368 tests，lint/typecheck 绿，demo 重建+zip 重打。同日回归加固：单测 +12（pull-to-refresh 16：亚像素边界 1px/2px、容器缺失回落、文档滚动橡胶带；window-infinite-scroll 8：滚动风暴重入门禁；mobile-torrents 39：失控根修核心性质×2——高内容初始仅 1 页+多轮速度轮询零 getList+reload 不链式；mobile-notifications 20：首屏 1 次+静默刷新零追加；delete-level-dialog 10：挂载级 UI 点击链/busy 禁用）+ e2e mobile-interactions 新增「空闲零 getList、滚动到底有界追加」（数据量门槛 ≤40 跳过，真栈 22437 条双浏览器 2 passed）；变异验证五类突变全被拦截：容器判定回退→4 例红、isNearViewportBottom 恒真→套件挂死（无限链自证）、终态去重回退→1 例红、原子替换回退→1 例红、v-infinite-scroll 回挂→源码契约红。终态 101 suites/1380 tests。 |
| 2026-09-05 增量（移动端验收修复） | ①下拉刷新误触发根修：`views/mobile/mixins/pull-to-refresh.ts` 滚动容器判定改为"`.mobile-content` 自身确实可滚（scrollHeight>clientHeight+1）才采用，否则回落文档滚动 `window.scrollY`"——实际布局（`.mobile-layout` min-height:100vh）下长列表滚动发生在 window，`.mobile-content` scrollTop 恒 0，修复前往意位置都被判"已到顶"、列表中部下滑约 120px 即触发整页 reload（12 个移动页面共用此 mixin 一处修复）；②移动四级删除补齐：新增 `views/mobile/components/DeleteLevelDialog.vue`（四等级选项+桌面同款文案二次确认，等级1 error 级）+ `views/mobile/delete-level.ts`（成功文案单源），列表页与详情页"删除"均改走 `deleteTorrentsWithLevel`（复用 `api/torrents.ts` 既有封装与 demo 桩既有 `/torrents/delete-with-level` 路由）；③刷新体验加固：`torrents.vue` `reload()` 原子替换（在途保留旧列表消除塌陷闪烁与滚动跳顶）、downloading 筛选终态整页刷新按 hash 去重（`terminalReloadedHashes`，runFilters/resetFilters 清空，防库内状态滞后时 10s reload 循环）。测试：pull-to-refresh 12（新增 window 滚动布局 3 例）/mobile-delete-level-dialog 6（新套件）/mobile-torrents 35（新增四级删除 5 例+终态去重/原子替换 2 例）/mobile-torrent-detail 10，前端全量 100 suites / 1357 tests，typecheck/lint 绿。 |
| 2026-09-11 增量（断速种子快照振荡双修复） | 用户实证（Console 挂桩 `getList` 调用栈 + `runtimeListMembership.observe` 新键）：断续下载种子每 ~5s 触发一次整表 getList，栈全走 `RuntimeListMembershipTracker.refresh` 新键路径。根因链：active-torrents 补查退避（2s）让断速种子在退避期轮次从快照缺席 → 前端完整快照基线被替换 → 回归轮被误判"新出现的未展示键" → getList，且该种子在分页/筛选外恒为未展示，循环不止。修复 A（backend `torrent_speed.py`）：`_TTLQueue` 条目新增 `last_supplement` 缓存，`get_disappeared` 返回 (待补查分组, 退避期缓存填充) 二元组，端点 `update_supplement_cache` 写回，速度恢复即弃缓存——退避只节流查询、快照成员不缺席；补查失败保留旧缓存撑连续，删除残留由 TTL 兜底。修复 B（frontend `torrentBatch.ts`）：`RuntimeListMembershipTracker` 新键判定加 `REAPPEAR_GRACE_MS=30s` 滞回（键掉出后宽限内回归判为抖动不触发，超宽限才重判新键；lastSeenAt 仅完整快照轮回收）。测试：后端定向 112 passed（含新增退避缓存填充/写回安全跳过 2 例），mypy/black/flake8 绿；前端 torrent-batch 127 passed（含新增宽限抖动/超期回归 2 例），lint 绿。 |
| 行号依据 | 全部由当前源码 grep / Read 实测，禁止沿用历史文档行号 |
| 覆盖深度 | 第一层（全部）+ 第二层（全部 16 个分支，含 frontend/demo）+ 第三层（2 个：torrent_crud.py、orphan_file_service.py） |
| 模板版本 | 后端 Python 四节；前端 Vue/TS 四节（适配 Options API + class-component 并存） |
| 本次新增 | 2026-09-22（W5 MCP 服务密钥批，feature `mcp-service-apikey-2026-09-22`，计划 PLANS/mcp-service-capabilities.md §12）：外部 agent 对接 MCP 的长期专用服务密钥——configs 第二个版本化键 `mcp.apikey.v1`（SHA-256 哈希=认证唯一事实源 + SM4 加密明文副本=控制面查看出口 + revision CAS + 归属追溯，无 Alembic 迁移）；`McpApiKeyService` 三态视图（absent/active/unreadable，secret_key 轮换后密文失效只影响查看、rotate 自愈）；控制面 `GET/POST /api/v1/mcp/apikey[/rotate]`（CommonResponse + reasonCode MCP_APIKEY_CONFLICT/INVALID；查看仅 active 写审计，rotate 记 previousOwner/rotatedBy 禁记密钥本体）；transport 认证新增 `btdmcp_` 前缀路径（errors.py 增 AUTH_API_KEY_INVALID 三件套），principal 内核抽 `build_principal_for_username` 两路共用（密钥路径不持原始密钥）；密钥入脱敏字典 + 泄漏扫描 canary；catalog `description_en` 成对下发（前端按 locale 选取，协议面工具描述维持中文）；设置页 MCP 面板服务密钥卡（端点/认证头提示、三态引导、掩码+复制、危险确认、409 重载、明文仅组件内存）+ zh/en 语言包 + demo 拦截层同形分支（固定演示密钥 + CAS）；威胁模型 S3/E3 与 key-rotation-runbook §2.1、mcp-runbook §3.1/§5/§6/§8.2 同步。验证：后端 test_mcp_apikey 21 例 + test_auth 扩 12 例 + contracts/redaction/enums 锚定，全量 pytest 5249 passed/0 failed、mypy/black/flake8 净；前端 mcp-settings 16 例 + demo-request 13 例，typecheck/lint/build/全量 Jest 125 套 1844 例/init.sh 全绿 / 2026-09-22（dev1.0.7 → dev 分支合并批，完整方案与验证证据见 PLANS/merge-dev107-into-dev.md 与 progress.md）：合入 MCP 服务全量（W0~W4-d：契约/SDK 探针/G0 门禁/Gate 骨架 + W1 配置控制面 + W2 同进程挂载/三重门禁/脱敏层 + W3 六工具（含 torrent_add_file 添加种子）+ W4 生命周期/升级矩阵/制品依赖回滚门禁 + G10 收官）、MoviePilot 整理联动（`moviepilot_instance`/`moviepilot_transfer_history` 两表——迁移 053003337878 重挂至 d1e2f3a4b5c6 保单 HEAD、integration/settings 服务、`/api/v1/moviepilot`、前端 MoviePilotPanel + 种子详情「媒体库」页签）、双语 P0/P1 文档线；i18n 实现取 dev P6-5 完成态（dev1.0.7 的 P1 期键结构弃用）；主机能力面板按 282f494 刻意移除（组件+spec+页签+roadmap 行四清）；torrent_add_service 保留 dev reasonCode 防泄露契约（固定 msg + logging.exception）并入 MCP 领域事实字段（info_hash/info_id/name/downloader_nickname/created）；i18n 审计门禁登记：MCP/MoviePilot 面板与 API 层入排除清单、媒体库页签/settings 页签 label 入白名单（双语另立项）；tests/core 四文件统一动态 head 集中方案 + moviepilot 幂等守卫；约束文档 HEAD 标注 053003337878；.gitignore 并集（+/data/）。（两侧合并前各自批次的完整变更日志保留于对应分支提交历史与第二层文件行，不在本单元格重复展开） |
2026-09-07 验收增量：修复 lifespan 局部导入遮蔽导致启动失败；前端刷新能力失败撤销旧授权；补齐 API/生命周期/缓存/AVD 回归，见 [测试覆盖](perspectives/test-coverage.md)。
