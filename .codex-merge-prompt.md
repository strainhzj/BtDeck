请将以下两个文档合并为一个完整的最终版本：

1. `backend/docs/style-and-contract-audit.md` - 原始评估报告
2. `backend/docs/style-contract-review.md` - 独立审查报告

## 合并要求

### 1. 保留原始报告的结构和内容
- 保留所有统计表格
- 保留所有问题描述和严重程度标记
- 保留所有修复建议

### 2. 从审查报告中补充以下内容

**补充遗漏的路径不匹配**（在"前后端路径不匹配"章节后添加）：
- `GET /setting-templates/{template_id}` - 后端不存在
- `GET /tracker-messages/statistics` - 后端不存在
- `POST /cronTasks/logs/cleanup` - 后端不存在
- `GET /articles` - 模板遗留，应删除
- `GET /setting-templates/{template_id}` 可能被 `/{log_id}` 动态路由误接收

**补充参数名和类型不匹配**（新增章节"参数与类型契约风险"）：
- `setting-templates apply` 的 body/path 参数双重不匹配
- `pageSize` vs `page_size` 参数名不匹配
- 模板应用请求 body 字段不匹配
- 下载器类型 `string`/`int` 混用
- 日期时间格式未统一

**补充未被前端调用的后端 endpoint**（新增章节"后端未使用 endpoint"）：
- 约 54 个 endpoint 未被前端调用
- 分类：死代码、预留接口、未接入 UI
- 列出高风险未使用接口

**修正 P0 方案的可行性评估**：
- OpenAPI schema 生成：先补高频接口的 Pydantic model
- HTTP status 与 code 同步：先做前端错误归一化
- 认证统一 HTTP 401：需先统一 ApiError 结构

**补充改进建议**：
- 建立机器生成的 API 对照表
- 旧路由做兼容期策略
- 分页字段渐进统一

### 3. 输出要求

1. 用中文撰写
2. 保存到 `backend/docs/style-and-contract-audit.md`（覆盖原文件）
3. 删除 `backend/docs/style-contract-review.md`
4. 保持清晰的章节结构
5. 不要修改任何业务代码文件
