请深入探索 BtDeck 项目的两个关键架构问题，生成详细的分析和修复方案。

## 任务一：双轨问题深度分析

### 1. 配置系统双轨
- 读取 backend/app/config.py 和 backend/app/core/config.py，逐行对比
- 找出所有引用这两个配置的地方（grep import 语句）
- 分析 docker-compose.yml 中的 DATABASE_URL 是否被实际消费
- 列出两个配置中所有冲突的默认值（特别是 SECRET_KEY、DATABASE_PATH 等）
- 提出合并方案：保留哪个、删除哪个、如何迁移

### 2. 数据库迁移双轨
- 读取 backend/app/main.py、backend/app/startup/lifecycle.py、backend/app/database.py
- 找出 init_db()、create_all()、Alembic 迁移的所有调用点
- 分析不同启动路径（开发环境 vs Docker vs PyInstaller）的初始化流程
- 画出当前的迁移流程图（文字描述）
- 设计统一迁移方案：如何从当前状态迁移到纯 Alembic

### 3. 残留路由文件
- 读取 backend/app/api/router.py 完整内容
- 与 backend/app/api/api.py 对比
- 分析是否有任何代码引用了这个旧文件
- 评估删除风险

## 任务二：定时任务代码执行隔离方案

### 1. 现状分析
- 读取 backend/app/tasks/cron_executor.py，找到所有 exec() 调用
- 读取 backend/app/tasks/enhanced_python_executor.py，分析沙箱机制
- 读取 backend/app/api/endpoints/cron_tasks.py，分析 API 层的权限控制
- 评估当前沙箱的防护能力：能否访问文件系统？网络？敏感模块？

### 2. 风险评估
- 列出绕过沙箱的可能方式
- 分析管理员账号泄露后的攻击面
- 评估对服务器的影响范围

### 3. 隔离方案设计
设计三个层次的隔离方案，给出具体代码示例：

**方案 A：白名单任务注册表**
- 定义任务类型枚举
- 预定义任务函数库
- 用户只能选择任务类型和参数

**方案 B：受限执行环境**
- 自定义 builtins 白名单
- 禁止危险模块导入（os, sys, subprocess, socket 等）
- 资源限制（CPU、内存、执行时间）
- 文件系统访问限制

**方案 C：进程级隔离**
- 使用 subprocess 在独立进程中执行
- 使用 Docker 容器隔离
- 使用 seccomp/AppArmor 限制系统调用

对每个方案给出：
- 实现复杂度（低/中/高）
- 安全等级（低/中/高）
- 性能影响
- 具体代码实现要点

## 输出要求

1. 用中文撰写
2. 保存到 backend/docs/architecture-deep-dive.md
3. 每个问题都要有：现状 -> 问题 -> 影响 -> 方案 -> 推荐
4. 不要修改任何业务代码文件
