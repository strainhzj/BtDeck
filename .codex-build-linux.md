# 任务：验证并修复 BtDeck Linux 构建

## 背景

BtDeck 是一个全栈 monorepo（Python FastAPI 后端 + Vue 2 前端），位于 `/home/huangzj/BtDeck`。
Linux 构建流程定义在 `deploy/build-linux.sh`，分为三步：
1. 构建前端（npm ci + npm run build）
2. PyInstaller 打包后端+前端为单个可执行文件
3. （可选）fpm 制作 .deb/.rpm

## 约束与注意

- 本机是 Debian Linux，Python 3.11.15，无 pip 模块（需用 uv 或 venv）
- npm 和 node 应已安装（请验证）
- 构建脚本会创建 `.venv-packaging-linux` 作为独立 venv，避免干扰项目别的 venv
- PyInstaller spec 文件：`deploy/btdeck.spec`
- 验证脚本：`deploy/verify-package.py`
- 如果缺少 fpm，跳过包构建步骤即可（这是可选的）
- **工作目录：** 在 `/home/huangzj/BtDeck` 下执行所有命令

## 执行步骤

1. 检查环境工具：python3, npm, node 是否可用，版本是否满足
2. 检查前端能否正常构建
3. 运行完整的 `deploy/build-linux.sh` 构建流程
4. 如果任何步骤失败，分析根因并进行修复
5. 修复后重新执行失败的步骤，直到构建通过

## 输出要求

- 每一步的执行结果（成功/失败 + 关键日志）
- 如果遇到问题，说明根因和修复方案
- 最终结果：dist/btdeck 是否成功生成并验证通过
